---
const { title, description, image } = Astro.props;
---

<div class="flex flex-col gap-1 ">
    <img class="h-[250px] object-cover rounded-md mb-3" src={image} alt={title} />
    <div>
        <h2 class="text-lg font-semibold whitespace-break-spaces">{title}</h2>
        <p class="text-[#876363] text-balance whitespace-break-spaces">{description}</p>
    </div>
</div>